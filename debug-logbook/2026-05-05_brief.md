# 2026-05-05 Brief Debug Log

Google Calendar sync was deployed and moved toward a backend-owned OAuth setup, with OAuth Client ID/Secret stored in Firebase Secret Manager instead of exposed in the CMS. The CMS also gained per-workshop `已完結課堂` handling and media picker image multi-upload, but follow-up review found important backend security, payment, registration-flow, visibility, and finished-class migration issues that still need fixing.
